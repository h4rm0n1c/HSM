Task 1 fixture

This fixture contains a deliberately buggy function (buggy.py) and a small
validation script (run_validation.sh). The harness will copy this fixture to a
temp run directory and the sample agent_patch.py will be applied in a no-model
test.
