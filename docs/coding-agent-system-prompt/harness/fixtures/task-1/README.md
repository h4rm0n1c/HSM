Task 1 fixture

This fixture contains a deliberately buggy function (buggy.py) and a small
validation script (run_validation.sh).

The TASK file contains the natural-language instruction for the agent.

For no-model testing (dry-run without an agent runner), use task-1-dummy
which includes a pre-written agent_patch.py.
